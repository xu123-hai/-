# 方案设计
## 1.功能需求分析
### 1.1 功能性需求
1. 用户基础信息录入模块：支持录入硬件参数、人员操作习惯信息，作为参数匹配模型的输入条件。
2. 人机运动性能虚拟测试模块
    - 定点定位测试：随机生成目标，通过位移碰撞触发检测完成命中判定，无需额外点击操作；具备正常倒计时计时功能，测试结束自动停止采集。
    - 动态目标追踪测试：运动目标做变速变向运动，采集用户跟随过程中的位置偏差数据。
    - 快速切换（Flick）响应测试：采集人员快速位移的响应时间、定位误差数据。
    - 物理位移标定测试：完成物理移动距离与虚拟视角转动关系标定。
3. 数据采集与统计模块：采集定位精度、响应耗时、追踪偏差、命中样本数量等原始运动数据，完成多组测试数据统计。
4. 参数匹配算法模块：将人员测试数据与录入的习惯参数加权运算，映射输出个性化控制参数，同时输出衍生评价指标。
5. 结果输出模块：展示计算得到的控制参数，支持一键复制参数数值，参数可直接导入控制应用中使用。
6. 测试重置功能：支持清空历史数据，重新开展多轮测试，方便多次采样取平均。

### 1.2 非功能性需求
1. 运行环境：纯前端单HTML文件，无需后端服务器，主流浏览器直接打开即可运行。
2. 实时性：虚拟测试场景帧率保持60FPS，碰撞检测、倒计时无卡顿卡死现象。
3. 易用性：测试流程引导清晰，操作门槛低；输出结果直观可读。
4. 稳定性：多次重复测试不会出现逻辑死循环、倒计时卡住问题；异常输入具备基础容错。
5. 可扩展性：可后续增加更多测试项目，优化参数匹配算法模型。

## 2.方案论证
本项目目标为实现人机协同场景下操作人员运动性能评估与个性化控制参数生成原型。有两套可选实现方案：

**方案一：本地桌面客户端程序（EXE程序）**
优点：系统底层访问能力强，数据采样精度高。
缺点：需要用户下载安装；跨平台兼容性差；需要学习桌面GUI框架；代码部署复杂，不便于原型快速验证；不符合本课程Web数字化仿真实践方向。

**方案二：基于Web浏览器纯前端实现方案（本项目选用）**
优点：
1. 单文件HTML，无需安装部署，浏览器直接运行，原型验证效率高。
2. 使用Canvas完成虚拟仿真场景，能够实现运动仿真、碰撞检测，对应数字化仿真课程技术。
3. 鼠标作为虚拟传感输入设备，完成人体手部运动数据采集，实现人机交互性能评估。
4. 所有数据计算全部在前端本地完成，不需要搭建数据库与后端服务器，开发工作量适中，适合课程设计。

缺点：浏览器环境下硬件底层访问存在限制。
综合课程设计目标，优先完成原型功能验证，因此选用**Web纯前端方案**。

## 3.方案规划
### 3.1 技术栈
- 页面结构：HTML5，搭建页面布局、测试容器、表单输入、结果展示模块。
- 页面样式：CSS3，完成页面布局、组件样式，测试界面视觉设计。
- 业务逻辑与仿真渲染：JavaScript，负责测试流程控制、倒计时逻辑、事件监听。
- 虚拟仿真绘图：HTML5‑Canvas API，实现虚拟测试场景渲染、目标绘制、自定义准星、碰撞检测动画。
- 数据处理：原生JavaScript完成统计运算、加权算法、参数映射，不引入额外框架。

### 3.2 需要配置的外部资源
本项目纯前端实现，**不需要外部服务器、不需要第三方后端接口**。
运行环境要求：Chrome、Edge等现代浏览器；无额外软件依赖，只需要浏览器即可打开HTML文件。

### 3.3 数据集来源
1. 实测采集数据集：系统运行过程中，由使用者完成各项虚拟测试，实时采集生成的运动行为原始数据（响应时间、定位偏差、追踪误差、命中样本），在浏览器内存临时存储。
2. 用户自定义输入数据：操作人员手动填写硬件参数、操作习惯参数。
>本原型不使用公开离线数据集，数据均为现场测试实时生成；如需后续迭代，可以将多次测试结果导出形成本地csv数据集。

### 3.4 前端UI方案
整体页面分为三大区域：
1. 基础参数录入区：表单控件，填写硬件、操作习惯相关信息。
2. 虚拟仿真测试区：Canvas画布作为核心测试窗口，内部渲染测试目标、自定义准星；测试倒计时文字提示，测试状态提示。
3. 结果输出区域：展示各项测试统计指标、最终生成的个性化控制参数，提供复制按钮。

交互逻辑：填写基础参数 → 选择测试项目启动测试 → 倒计时完成开始测试 → 自动采集数据 → 测试结束展示本次测试得分；全部测试完成后运行算法生成最终参数。

### 3.5 后端算法方案
>本项目无后端服务，全部算法运行在前端浏览器。
1. 数据预处理算法：剔除测试过程中异常离群样本，对多轮测试结果求取平均值，降低偶然操作带来的误差。
2. 多维度加权评分模型：对定位精度、追踪偏差、响应速度不同维度设置权重，结合操作人员握鼠、发力习惯等输入参数。
3. 参数区间映射算法：将人机运动性能评分映射到合理的控制参数数值区间，输出可用控制参数，同时附带衍生指标计算。
4. 标定换算算法：完成物理位移‑虚拟转动量换算，输出cm/360等效参考指标。

## 4.技术路线
1. 需求梳理与原型界面搭建：搭建HTML基础页面布局，划分输入区、仿真画布区、结果区，编写CSS页面样式。
2. Canvas虚拟仿真模块开发：实现画布、自定义准星绘制；开发目标随机生成、目标移动动画逻辑。
3. 检测逻辑开发：实现鼠标位置监听、碰撞触发命中检测；编写倒计时逻辑，解决计时卡顿卡死问题。
4. 测试业务流程开发：分别开发定点定位测试、动态追踪测试、快速切换测试、物理标定测试；完成各项测试原始数据采集。
5. 数据处理与算法开发：编写样本预处理、加权评分、参数映射换算算法。
6. 结果展示功能：完成指标可视化、一键复制参数功能、重置功能。
7. 整体联调测试：修复倒计时、碰撞检测bug，验证输出参数是否落在有效可用区间。
8. 文档整理，项目文件提交代码仓库。

## 5.计划安排（时间规划）
|时间节点|工作内容|
| ---- | ---- |
|第1周|完成选题确定，编写选题说明、方案设计文档，提交Git仓库；完成页面基础布局。|
|第2周|开发Canvas仿真模块，实现画布、准星、目标动画、碰撞检测、倒计时功能，完成基础测试单元。|
|第3周|完善全部4套人机性能测试模块；开发数据采集统计、参数匹配算法逻辑。|
|第4周|完善UI交互、结果展示、复制功能；整体调试修复逻辑bug，验证输出参数有效性。|
|第5周|系统整体测试，整理项目文档，完善README，完成项目最终交付。|

## 6.参考文献
[1] 张成孝. 人机交互技术[M]. 机械工业出版社.
[2] 王健. 数字化仿真技术基础[M]. 高等教育出版社.
[3] 李刚. HTML5与Canvas图形编程实战[M]. 人民邮电出版社.
[4] 李方园. 智能制造中人机协同技术发展综述[J]. 制造业自动化.
[5] MDN Web文档. Canvas API 开发手册[EB/OL]. https://developer.mozilla.org/zh‑CN/docs/Web/API/Canvas_API
