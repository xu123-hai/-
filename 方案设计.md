
# 方案设计
## 一、功能需求分析
### 1.1 数据处理模块
- 读取工业传感器时序数据（切削力、振动、声发射）；
- 缺失值、异常值检测（IQR方法），数据标准化（Z‑score）；
- 提取时域（均值、RMS、峭度、偏度）、频域（FFT）特征；
- 数据集划分：训练集、验证集、测试集，使用K折交叉验证。

### 1.2 算法模型模块
- 磨损分类任务：输入信号/特征 → 输出3类磨损等级（轻微/中度/严重磨损）；
- 寿命预测任务：输入时序退化序列 → 输出RUL剩余使用寿命；
- 输出评估指标：分类任务（准确率、精确率、召回率、F1、混淆矩阵）；回归任务（RMSE、MAE、R²）。

### 1.3 前端可视化模块
- 原始传感器信号波形展示；
- 当前刀具磨损等级、预测RUL数值展示；
- 严重磨损状态下触发报警提示；
- 历史磨损趋势简单展示。

### 1.4 非功能需求
- 模型可复现，代码注释完整；
- 文档齐全，可在Git仓库查看版本记录；
- 算法可在普通PC完成训练，不需要高性能服务器。

## 二、方案论证
方案A：只使用传统机器学习SVM做磨损识别。
优点：训练速度快；缺点：需要大量人工构造特征，对长时序退化趋势捕捉能力弱，RUL预测效果差。

方案B：CNN‑LSTM混合深度学习模型（本项目选用）
一维CNN负责从原始振动信号自动提取局部波形特征，LSTM挖掘长时间序列退化变化趋势；同时完成磨损分类+RUL回归预测，减少人工特征工作量，更适配工业传感器时序数据；课程中已经学习CNN、LSTM、BP反向传播、PHM‑RUL相关理论，知识匹配度高，适合课程大作业实现。

## 三、方案规划
### 3.1 技术栈
- **后端算法**：Python；numpy、pandas做数据处理；scikit‑learn做特征工程、评估指标；PyTorch搭建CNN‑LSTM网络；matplotlib绘图。
- **前端UI**：HTML+JavaScript（简易网页，读取算法输出结果做可视化，复用课程思维导图页面类似架构）。
- **版本管理**：Git + GitHub/Gitee仓库，提交`选题说明.md`、`方案设计.md`、源码、运行结果文件。

### 3.2 需要配置外部资源
1. Python环境，安装pandas、numpy、scikit‑learn、torch、matplotlib；
2. Git环境，用于仓库提交推送。

### 3.3 数据集来源
采用**2010 PHM Society刀具磨损公开竞赛数据集**，工业铣刀全生命周期传感器数据，包含三轴切削力、三轴振动、声发射信号，附带刀具后刀面磨损量标签，适合磨损分类和RUL预测任务。
> 获取来源：PHM‑Society公开数据集仓库。

### 3.4 后端算法方案
1. 数据预处理：IQR检测异常值，Z‑score标准化；滑动时间窗口切分时序样本；
2. 特征：同时保留原始信号序列 + 手工时域频域特征；
3. 网络结构：1DCNN层（卷积+池化提取局部特征）→ LSTM层（时序退化学习）→ 双输出头：
   - 输出头1：Softmax，磨损三分类；损失：交叉熵损失；
   - 输出头2：线性输出，RUL回归预测；损失：均方误差MSE；
4. 训练策略：BP反向传播，梯度下降更新权重；K折交叉验证调参；
5. 模型输出磨损等级、RUL值；超过阈值输出报警标记。

### 3.5 前端UI方案
简单网页，实现：
1. 上传/读取算法输出的结果JSON；
2. 绘制传感器波形曲线；
3. 面板展示磨损等级、RUL数值；
4. 若判定严重磨损，红色报警提示；
5. 简单历史趋势展示。


